import os
from setuptools import setup

setup(
    name = "logparser",
    version = "0.0.4",
    author = "logpai",
    description = ("Log parsing"),
    license = "MIT",
    keywords = "log parsing",
    packages=['logparser', 'demo', 'benchmark'],
)
